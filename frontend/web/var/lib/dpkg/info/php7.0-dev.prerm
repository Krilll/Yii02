#!/bin/sh

set -e

if [ "$1" = "remove" -o "$1" = "purge" ]; then
    update-alternatives --remove php-config /usr/bin/php-config7.0
    update-alternatives --remove phpize /usr/bin/phpize7.0
fi



exit 0
